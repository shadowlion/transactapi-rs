[package]
edition = "2021"
name = "transactapi-rs"
version = "0.2.0"

[lib]
path = "src/lib.rs"

[dependencies]
reqwest = {version = "0.11", features = ["json"]}
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
